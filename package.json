{
  "name": "listen-x",
  "version": "0.1.0",
  "description": "My Chrome Extension",
  "private": true,

  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "live-pug": "cd browserAction && pug pugs index.html -o . -w",
    "live-scss": "cd browserAction && sass scss:css -w -s compressed",
    "eslint": "eslint ./browserAction/ --fix",
    "stylelint": "stylelint \"./browserAction/**/*.scss\" --fix"
  },
  "author": "Gerrnperl Lunn",
  "license": "GPL-3.0-or-later"
}
